$(document).ready(function() {
  $("nav .login").on("click",() => {
    const $loginId = $(this).
    $.ajax("/login/:id", {
      method:"POST",
      data:{todoId: todoId}
    }).then(() => {

    });
  });
});
